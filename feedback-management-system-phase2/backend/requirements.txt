# Phase 2 dependencies (Python 3.10 – 3.14 supported)
# Pinned to versions that import cleanly on Python 3.14 (older SQLAlchemy/Pydantic crash).
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0.41
pydantic>=2.9.2
python-multipart>=0.0.12

# Phase 2 — ETL pipeline
pandas>=2.2.3
numpy>=2.1.0
openpyxl>=3.1.5
